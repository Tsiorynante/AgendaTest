{% block stylesheets %}
    <style>
        input,textarea,select{
            width: 100%;

        }
        #body{
            background-color: rgb(0,102,102);
        }
        #btn{
            background-color: rgb(255,178,102);
        }
        #card{
            background-color: rgb(7,238,222);
        }

    </style>
{% endblock %}
{% block body %}

<div class="container">
		<div class="row justify-content-md-center">
			<div class="col col-lg-6 mt-4">
				<div class="card p-4" id="card">
					<h4 style="text-align: center">{{text_label|default('Formulaire')}} </h4>
                    <hr>
					{{form_start(form)}}
                        {{ form_widget(form)}}
                        <button class="btn btn-primary col-12" id="btn">{{button_label|default('Save')}} →</button>
                    {{ form_end(form) }}
					<hr>
				</div>
			</div>
		</div>
	</div>

{% endblock %}